name: Diagnostic Build
on: [push]
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Show what is inside the repository
        run: |
          echo "محتويات المجلد الحالي:"
          ls -R
